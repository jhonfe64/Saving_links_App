<%-include("../layouts/header", {title: 'add'}) %>
<%-include("../partials/navigation") %>
    <div class="conatiner p-4">
        <div class="row">
            <div class="col-md-4 mx-auto">
                <div class="card">
                    <div class="card-body">
                        <!-- ojo cuando se envien los datos del form los enviará a la ruta /links/edit/+ el id  en routes/links -->
                        <form action="/links/edit/<%= link.id %>" method="post">
                                <div class="form-group">
                                    <input value="<%= link.title %>" type="text" class="form-control" name="title" placeholder="title" autofocus> 
                                </div>
                                <div class="form-group">
                                    <input  value="<%= link.url %>"  type="url" class="form-control" name="url" placeholder="url"> 
                                </div>
                                <div class="form-group">
                                    <textarea name="description"  rows="2" class="form-control" placeholder="Description"><%= link.description %></textarea>
                                </div>
                                <div class="form-group">
                                    <button class="btn btn-success btn-block">Save</button>
                                </div>    
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
<%-include("../layouts/footer") %>    